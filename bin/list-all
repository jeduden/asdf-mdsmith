#!/usr/bin/env bash
set -euo pipefail

repo_url="https://github.com/jeduden/mdsmith.git"

git ls-remote --tags "${repo_url}" \
  | awk '{print $2}' \
  | sed -e 's#^refs/tags/##' -e 's#\^{}##' \
  | grep -E '^v?[0-9]+\.[0-9]+\.[0-9]+$' \
  | sed 's/^v//' \
  | sort -uV
